# GitNexus — Code Intelligence

The canonical GitNexus workflow for this repository lives in `.claude/skills/gitnexus/README.md`.

This file remains as a compatibility entrypoint for tools that expect a root-level `CLAUDE.md`, which keeps one maintained source of truth and prevents instruction drift.
